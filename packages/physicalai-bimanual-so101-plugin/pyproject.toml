[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "physicalai-bimanual-so101-plugin"
dynamic = ["version"]
description = "Bimanual SO-101 robot arm plugin for PhysicalAI (twin 6-DOF STS3215 arms)"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
    "physicalai>=0.1.1",
    "numpy>=1.24",
    "loguru>=0.7",
    "pydantic>=2.0",
    "feetech-servo-sdk>=1.0",
]

[project.optional-dependencies]
tests = ["pytest", "pytest-asyncio"]

[project.urls]
Homepage = "https://github.com/MarkRedeman/physicalai-rebot-b601-plugin"
Repository = "https://github.com/MarkRedeman/physicalai-rebot-b601-plugin"
Issues = "https://github.com/MarkRedeman/physicalai-rebot-b601-plugin/issues"

[project.entry-points."physicalai.studio.catalog_plugins"]
bimanual-so101 = "physicalai_bimanual_so101_plugin.studio_catalog:register_physicalai_studio_plugin"

[tool.hatch.version]
source = "vcs"
raw-options = { search_parent_directories = true }

[tool.hatch.build.targets.wheel]
packages = ["src/physicalai_bimanual_so101_plugin"]

[tool.hatch.build.targets.wheel.force-include]
"urdf" = "urdf"

[tool.hatch.build.targets.sdist]
include = [
    "src/physicalai_bimanual_so101_plugin/**",
    "urdf/**",
    "README.md",
    "pyproject.toml",
    "LICENSE",
]
